<!-- ---
//Basically, put a pause on this until Bridgette gives the go-ahead for using React libraries.

//this is our hero section; hours tab will also go in here

import EmblaCarousel from '../ui/EmblaCarousel';
import HoursPanel from '../ui/HoursTab';    
import Button from '../ui/Button.astro';
import ProfileCard from '../ui/ProfileCard.astro';

// /slide-1 works because astro serves public at the root
const slides = [
  { img: '/slide-1.webp' },
  { img: '/slide-2.webp'},
  { img: '/slide-3.webp'}
];

const options = {
  loop: true,
  align: 'start'
};


---

<section class="hero-section">
  <EmblaCarousel 
    client:load 
    slides={slides} 
    options={options} 
  />
 <HoursPanel client:load apiUrl="/api/hours" />

</section>

<style is:global> /* is global so it applies to the classes in jsx */
  .hero-section { max-width: 100%; }
  .embla {
  max-width: 48rem;
  margin: auto;
  --slide-height: 19rem;
  --slide-spacing: 1rem;
  --slide-size: 100%;
}
.embla__viewport {
  overflow: hidden;
}
.embla__container {
  display: flex;
  touch-action: pan-y pinch-zoom;
  margin-left: calc(var(--slide-spacing) * -1);
}
.embla__slide {
  flex: 0 0 var(--slide-size);
  min-width: 0;
  padding-left: var(--slide-spacing);
}
.embla__slide__number {
  border: 0.2rem solid var(--detail-medium-contrast);
  border-radius: 1.8rem;
  font-size: 4rem;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  height: var(--slide-height);
  user-select: none;
}

</style>
 -->

 <!-- Just making a really basic  -->
---
// src/components/Hero.astro

import Link from '../ui/Link.astro';
---

<section>
  <div>
    <h2>
      Little Wolf Restaurant
    </h2>
    <p>
      Exceptional dining experiences with seasonal ingredients and timeless hospitality.
    </p>
    <div>
      <Link href="/reservations" size="lg" label='Reserve' variant='outline'>
    </div>
  </div>
</section>
